<!--<div class="card-product m-3">
  <%= cl_image_tag @cone.photo.key %>
  <div class="card-product-infos">
    <h2><%= @cone.name %></h2>
    <p><%= @cone.description %></p>
    <p>Price: <%= @cone.price %></p>
    <p>Condition: <%= @cone.condition %></p>
    <p>Location: <%= @cone.location %></p>
  </div>
</div>-->


<div class="container mt-5 mb-5">
  <div class="row">
    <div class="col-8">
      <div class="cone-show-card">
        <h1><%= @cone.name %></h1>
        <p><%= @cone.description %></p>
      </div>
      <div class="cone-show-card">
        <div><img src="cone.png"></div>
      </div>
    </div>

    <div class="col-4">
      <div class="cone-show-card">
        <h5>Book this cone</h5>

        <form action="">
          <div class="input-group">
            <span class="input-group-text" id="start-date-label">Start:</span>
            <input type="text" class="form-control">
          </div>
          <div class="input-group mt-2">
            <span class="input-group-text" id="end-date-label">End:</span>
            <input type="text" class="form-control">
          </div>
          <input type="submit" value="Book" class="btn btn-success mt-2" style="width: 100%">
        </form>
      </div>
      <div class="cone-show-card">
        <h3>Details</h3>
        <div class="flex">
          <div class="flex">
            <%= @cone.condition %>
          </div>
          <div class="flex ms-3">
            $<%= @cone.price %>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>

<div style="width: 100%; height: 600px;"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>"
></div>
